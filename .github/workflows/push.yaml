name: Test
on:
  push:
    branches:
      - main
jobs:
  test:
    name: Run test suite
    runs-on: self-hosted
    container: maven:latest
    # env:
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - run: mvn test